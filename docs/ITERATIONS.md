# Parlor 自由桌面迭代

方向：给朋友一张可以共同布置、随便玩的 2D 桌面。保留绿色桌布与实体物件感，资源库提供可以反复取用的模板，桌上物件各自独立。玩法由玩家约定。

## 原始问题

资源入口会替换整个房间；卡牌、牌背和拖动仍围绕单一中央牌堆。离线试玩维护另一套规则，新增能力容易漂移。样式积累到 5575 行，多轮覆盖相互影响。

此次界面按功能工作台处理，保留 Parlor 名称、绿色桌面和本地 Phosphor 图标。入口采用真实组件预览；世界中减少装饰和常驻说明，让操作随选择出现。

## 第一轮：卡组与统一引擎

- 重叠卡组直接整体拖动，跨区域转交保持私有投影，整组操作可一步撤销。
- 浏览器引擎由 Node 权威核心确定性生成；单机模式只保留适配层。
- 手牌、洗牌、收回、离席与重置维持权限和实例重新编号。
- 公开牌的连通组按状态建立空间索引，避免每张牌重复扫描整桌。

## 第二轮：资源实例与开放空间

- 多个牌盒并存、独立位置和牌背、按原牌盒回收。
- D6、D20、计分器、便签、收纳袋、棋盘、桌垫、棋子与筹码。
- 复制、位置锁定、编辑、移除；资源模板可以重复取用。
- 袋子只公开数量，存入卡牌重新编号，摸出卡牌进入操作者的私有手牌。
- 可放置范围从主桌扩展至 10800 × 7100；本地镜头、桌垫导航与小地图独立于同步状态。
- 桌面存档将手牌归回牌盒，保留资源、布置和计数，不保存会话、身份或聊天。

## 第三轮：界面与操作闭环

- 收拢为统一的绿色桌面、纸质资源库与紧凑工具栏，样式整理至约 630 行。
- 搜索、分类、收藏、浏览器 JSON 导入，点击添加和拖出添加使用同一条命令路径。
- 牌盒轻点选择、双击抽牌、拖动移位；拖进手牌区才抽出一张。
- 卡组优先识别袋子和手牌目标，避免开放世界范围吞掉区域操作。
- 固定手牌抽屉、物件编辑、卡牌放大、房间聊天、桌边状态、本机存档及文件导入导出。
- 手牌和牌背按资源实例渲染；未变化的卡牌 DOM 保留，减少图片重复加载。
- 命令回执带操作者可见的状态，避免 SSE 延迟时存档恢复后的导航仍使用旧布置。
- 修正锁定卡组、32 副牌容量边界、存档恢复层级、共享计数与移动端输入时侧栏被 resize 关闭的问题。

## 第四轮：取消、锁定与导入资源管理

- 锁定公共牌会阻止整组操作、一键整理和批量收回；失败操作不改变资源、历史或撤销栈，解锁后正常恢复。
- 资源库拖动在 Escape、取消、丢失指针捕获、窗口失焦和关闭面板时清理，不会因后续松手或点击再添加物件；落在其他面板或控件上不提交。
- 触屏资源预览保留原生纵向滚动和轻点添加，鼠标仍可拖出取用。
- 每次导入牌盒保存独立本机记录；同 ID 的内置包、多份导入和旧版记录均可访问，移除某份导入不影响已上桌的实例。

## 第五轮：减少常驻文案

- 移除桌面底部说明栏、顶部重复资源说明、资源库介绍、资源卡功能段落，以及物件上的双击教学文字。
- 入口保留品牌、标题、选牌与开始；物件预览直接使用组件。便签默认留白。
- 资源库在桌面和手机上均默认收起，手动点击或按 B 打开。
- 保留资源名、牌数、锁定、连接等必要状态；长说明集中到帮助，动作提示放入悬停和确认。
- 已通过 104 项自动化测试与静态构建，真实浏览器视觉验收仍待恢复工具。

## 第六轮：紧凑导航与不中断的操作（0.4.0）

- 左侧保留资源、世界、聊天、撤销、标记和工具；历史移入工具，关闭时焦点回到可见的工具按钮。
- 顶栏压缩至 58px，侧栏按内容取高，资源收藏和移除按钮在悬停或键盘聚焦时出现，触屏保持可见。
- 抽牌、翻面等常用动作直接呈现，旋转、转交和收回散牌等放入「更多」。Escape 先关闭菜单，保留选择。
- 聊天、玩家状态等无关更新保留正在打开的菜单、键盘焦点和转交对象，避免操作被打断。
- 缩放、全景和小地图集中到右下角；小地图默认关闭，支持键盘与手机按需打开，沉浸模式也可主动查看。
- 镜头取景和资源落点按实际资源库宽度避让；地图与面板操作不改变共享桌面版本。
- 已通过 107 项自动化测试、静态构建与差异格式检查。

## 第七轮：手机镜头与手牌手势（0.5.0）

- 双指按指间中心移动和缩放镜头；第二根手指落下时取消物件拖动，避免把缩放变成出牌或移牌。
- 支持第三根手指加入、换指和抬起一根后继续单指移动；取消、失焦和指针捕获丢失时清理手势，下一次操作可以正常开始。
- 手机手牌横向滑动交给浏览器滚动，轻点选择，纵向拖出出牌；关闭手牌或加入第二根手指会取消正在准备或进行的拖牌。
- 新牌到来时保留已有手牌 DOM、焦点、滚动位置和正在拖动的牌。小地图关闭时不再执行绘制。
- 操作说明只更新帮助页，主界面不增加常驻教学文字。
- 新增 6 项生产脚本事件测试；全量 113 项测试通过。原生触摸行为仍需真实手机浏览器验收。

## 第八轮：连续取用与准确选择（0.6.0）

- 资源取用按点击顺序排队，预先保留不同落点；原按钮用数量显示等待中的取用，连续点击不会丢失。
- 服务端与试玩回执返回准确的实例 ID。另一位玩家同时添加资源时，不会选错物件。
- 延迟回执尊重用户后来选中的资源、面板和视角；离线或切换身份时取消尚未发出的取用。
- 失败会清理等待数量并继续下一项，卡牌定义与私有正面不进入创建回执。
- 新增 8 项回归测试；全量 121 项测试通过。

## 第九轮：同桌反馈与可检索历史（0.7.0）

- 操作和远端聊天在顶部浮现，使用玩家颜色；点击直达历史或聊天，分别在 4.2 / 7.2 秒后清除。状态回执和 SSE 不会重复弹出，入座和切换视角不重放旧消息。
- 洗牌广播一次经过过滤的事件，只包含世界坐标；客户端播放 0.8 秒叠牌动画，兼容减少动态效果。
- 历史从工具移到常用侧栏，L 开关；保留最近 500 条，按玩家、类别、关键词筛选，可导出当前结果。使用稳定玩家 ID，改名后仍能追踪之前的操作。
- 点击昵称即可编辑；重名自动加序号，移除桌边状态。改名不影响席位、会话、手牌或桌面撤销。
- UNO 牌面去掉英文缩写与重复小字，使用数字与符号；保留完整中文无障碍名称。
- 新增消息去重、自动清理、点击跳转、过滤、改名、记录隐私与 UNO 渲染测试。完整需求继续记录在 [EXPERIENCE-PLAN](./EXPERIENCE-PLAN.md)。
- 撤销保留原操作并追加关联记录，避免审计记录被一起撤掉。
- 2026-09-07：全量 127 项测试、静态校验 153 个 DOM ID、构建与 `git diff --check` 通过。真实浏览器仍被审批服务 404 阻断。自动 Git 提交的权限请求也被同一接口 404 拒绝，改动保留在工作区和 `.iterations/2026-09-07/0.7.0/` 检查点中。

## 第十轮：自由私人区与轻量实时拖动（0.8.0）

- 私人区按稳定席位分布在主桌周围，私有牌具有持久坐标和角度；新玩家加入、继续抽牌、改名和重新入座都不会重新挤排列好的牌。手牌抽屉提供「到私人区」和「整理」。
- 点击朋友的牌背可连续标记最多 12 张，使用请求者的指针颜色，8 秒自动清除；持有者收到交付按钮。交付由持有者发起，一次完成且可撤销；房主不能代替持有者接受请求。
- 选牌标记不进入桌面状态、历史或撤销栈，只有 ID、颜色和期限；已过期、已移动或锁定的牌会让交付整体失败。
- 拖动限频为每秒 10 次；整叠牌的消息改为单个引用与牌数，不再逐帧发送全部 ID。服务端缓存叠牌拓扑，客户端复用拖动 DOM 并过渡位置。
- 持续拖住时每 500 ms 保活；每次拖动拥有独立 ID，服务器过滤松手后迟到的旧坐标，新拖动不会被旧结束消息清掉。
- 139 项全量测试通过，包含双引擎私人摆放、交付与权限测试、生产拖放和高亮清理测试、晚到消息与 108 张整叠牌数据量检查。108 张牌的单个拖动事件小于 500 字节；8 人同时以 10 Hz 拖动、向另外 7 人转发，纯事件负载上界约 280 KB/s，HTTP/TLS 开销另计，实际网络延迟待真人联机测量。
- 构建和差异检查通过；Git 与真实浏览器审批通道仍未恢复，检查点保存在 `.iterations/2026-09-07/0.8.0/`。

## 第十一轮：完整对局、自动存档与恢复身份（0.9.0）

- 完整对局格式保留手牌归属与坐标、原牌序、席位、计数、袋中物件、本桌收藏、聊天与审计记录。与原有「布置模板」分开；恢复完整对局前先在本机与房主磁盘备份当前桌面。
- 房主自动存档采用 0600 文件、0700 目录、临时文件同步后原子替换，保留上一份 .bak。阻止同一份房间文件被两个开房进程同时使用，写盘错误显示为未保存，恢复后可重试。
- `--resume-latest` / `--resume <文件>` 和双击脚本的 0 选项可续局。新隧道地址需重新分享；个人口令只恢复指定席位，不出现在别人投影和普通邀请链接里。
- 每项客户端操作持有唯一编号；回执随桌面落盘，丢失响应、并发重复请求和重启后的重试不会重复执行。回执超出保留窗口或来自恢复前桌面的旧操作会拒绝。异步读取牌盒不阻塞朋友的其他操作。
- 浏览器保留自己的最后确认视图、镜头和席位。断线后桌面持续可见，暂停修改并自动恢复；同一设备有多个身份时先选择席位。待确认操作跨刷新继续核对。
- 服务重启及完整对局恢复会清空撤销链，已发生的历史保留。完整对局文件包含私有牌面与续局口令，仅供房主保管；外部图片仍需原资源目录。
- 155 项自动化检查通过，构建、脚本语法与差异格式通过。另 1 项真实 HTTP/SSE 测试因本地监听权限未运行；审批接口返回 `codex-auto-review` 404。真实渲染、触摸与公网体验仍待验证，未计作通过。Git 提交仍受相同审批阻断，保留累计补丁检查点。

## 第十二轮：可续局的德州扑克（0.10.0）

本节是历史实现记录；0.13.0 已按用户要求移除自动规则与专用面板，德州改为普通桌游资源。

- 独立 52 张牌与德州桌垫，支持 2–8 人、庄家轮换、单挑盲注顺序、烧牌、四轮下注与摊牌；侧栏「德州」和 T 打开，P 保留位置标记。
- 无限注行动、最小加注、不足额全下后的加注权、累计加注重开、未跟注退款、主池和边池、七选五牌型和平分零头。只剩一人有筹码时不能向无人可跟的边池下注；不足大盲的全下按实际金额匹配。
- 手机面板包含自己的底牌、公共牌、筹码、下注预设与当前行动者。输入草稿和焦点不被聊天刷新打断。无人跟注胜出不强制亮牌，玩家可以自愿亮牌。
- 私人底牌仍可摆放与旋转，其他受管理的牌禁止手动抽取、复制和转交；工具栏禁用冲突操作并解释原因。布置模板解除专用牌盒与桌垫的管理锁，完整对局则保留规则进度。
- 下一手延续筹码，可补充筹码或下手休息；房主可为确实离线的当前行动玩家代为弃牌，记录明确归属房主。下注阶段、手牌与筹码随完整存档和服务重启恢复。
- 行动轮次防止迟到的下注或关闭操作作用于后来牌局；撤销更新桌面代次，旧回执仍能去重，未知旧命令被拒绝。反复用个人链接恢复时最多保留每席位最近 16 个会话，不会导致续局文件无限增长。
- 2026-09-07：185 项检查通过、1 项真实 HTTP/SSE 检查因监听权限跳过；构建、203 个 DOM ID 静态契约、开房脚本语法与差异格式通过。新增规则测试在服务端和生成的浏览器引擎执行，包含 2–8 人各三手变化行动、每步筹码守恒和存档有效性。生产界面脚本走完整手，但没有真实布局或触摸证据。
- Git 写入和真实浏览器仍被审批接口 404 阻断。累计检查点保存在 `.iterations/2026-09-07/0.10.0/`，相对 `e1d7b45`，不能与旧累计补丁顺序叠加。

## 第十三轮：试玩自动续局与多页保存（0.11.0）

- 试玩自动保存完整房间检查点和本机镜头，刷新后保留牌 ID、原牌序、手牌位置、私有投影、聊天与德州行动。加载成功后清空撤销链；已有操作不重放通知。
- 每次操作后立即写入；镜头变化合并为 300 ms 后保存，切到后台主动写入。失败保留正在玩的桌面、明确显示错误和重试入口；未保存的改动离页时提醒。
- 同一试玩由多个标签页修改时，IndexedDB 在一个写事务中核对版本；过期写入自动另存为独立副本，原记录不被覆盖。存档面板列出「其他试玩」供切换，切换前先保存本页。
- 「新开试玩」先保留完整的「新开前」备份；备份失败不重置桌面。重开和恢复期间暂缓新的桌面动作，避免慢写入期间的改动落在已备份的旧桌面上；延迟写入检查通过。损坏的自动记录也不会被空白桌面覆盖。
- 恢复多人对局后，试玩视角遍历实际席位；可保存当前查看身份，刷新继续正确的私有投影。普通试玩分享链接不包含本机副本编号。
- 2026-09-07：191 项通过、1 项真实网络检查跳过；构建、209 个 DOM ID 静态契约、脚本语法与差异格式通过。新增生产脚本检查覆盖刷新、镜头后台保存、并发写入分流、存储失败重试、备份失败不重开、损坏记录保留和导入三人对局的视角恢复。
- IndexedDB 检查采用能模拟请求顺序与事务回滚的存储替身，不能替代浏览器实际耐久性测试。真实浏览器、本地监听和 Git 写入仍受同一审批通道阻断。累计检查点在 `.iterations/2026-09-07/0.11.0/`，未提交。

## 第十四轮：多人德州续局串联验收（0.11.1）

- 三份独立生产客户端通过实际传输层进行三人德州：发牌、各轮跟注与过牌、公共牌、摊牌、分配筹码和下一手均由界面事件驱动。所有客户端看到相同的公共状态，每次操作后筹码守恒。
- 检查实际发给各玩家的 SSE 状态和命令回执：每人只得到自己的未公开私牌正面和续局口令，烧牌正面与牌堆内容不可见；摊牌后正面公开，下一手重新编号。
- 加注响应丢失后，让其他两人继续下注，再离线刷新原客户端。最后保存的私有投影保留，重连以原编号核对加注，即使行动轮次已变化也只确认已有回执，不再扣筹码或追加历史。
- 使用真实磁盘自动存档，在翻牌圈下注后丢失回执并关闭传输层，从检查点重建房间。三客户端恢复原席位、私牌、牌序、下注进度和持久回执，随后完成剩余牌局；没有重复下注。
- 2026-09-07：194 项通过、1 项真实网络检查跳过；构建、209 个 DOM ID 静态契约、开房脚本语法与差异格式通过。本轮未发现新的产品缺陷，新增三项串联回归检查。
- DOM、IndexedDB、HTTP 请求和 SSE 流仍使用替身；磁盘文件使用实际读写。真实浏览器、本地套接字与 Git 写入仍受审批接口 404 阻断，不能据此宣告联机验收完成。累计检查点在 `.iterations/2026-09-07/0.11.1/`，未提交。

## 第十五轮：光标回归与启动链接（0.11.2）

- 核查旧故障链：本机光标层仍位于变换世界之外，原生光标未被 CSS 隐藏；SSE 暂断不直接禁用操作，光标请求保持合并、串行发送与 1.8 秒超时。生产事件和协议替身未复现原来的锁死，真实线上复现仍待验。
- 修复公网启动在自动打开浏览器后不显示房主链接的问题。本地、公网和 `--no-open` 都同时打印「房间加入链接（发给朋友）」及「房主链接（仅自己使用）」。
- Node 服务和静态构建按文件内容生成脚本/样式版本链接；入口要求重新校验，Node 的脚本/样式也不再沿用五分钟新鲜期，避免刷新后仍引用缓存中的旧客户端。
- 新增四项光标回归：1440/390 宽度下导航、关闭面板和失焦后继续拖动抽牌；双客户端的 SSE 暂断与恢复；慢光标请求合并、超时及同时抽牌。另新增资源版本稳定性和入口缓存校验两项检查。
- 2026-09-07：200 项通过、1 项真实网络检查跳过；构建、209 个 DOM ID 静态契约、12 个构建资源内容版本、开房脚本语法与差异格式通过。用户再次授权后的只读浏览器检查和真实 HTTP/SSE 测试仍被审批接口的 `codex-auto-review` 404 拒绝，未修改系统或重启旧服务。
- 本轮补丁基于已核验的 `a35a28a`（`ver3`，0.11.1），保存在 `.iterations/2026-09-07/0.11.2/`；它是该基线之后的增量，不与旧累计补丁顺序叠加。

## 第十六轮：真实双人验收与缩放显示（0.11.3）

- 当前完整权限下，浏览器、本地监听与 Git 写入均可用；0.11.2 成功提交为 `d7a8c0e`，此前审批失败记录保留为历史。
- 真实主客标签通过抽牌与私有投影、私牌自由拖动、同色选牌请求及过期、持有者转交、聊天通知、改名、历史玩家筛选、重开席位和完整存档恢复。恢复前自动备份在真实 IndexedDB 中可见。
- 发现小地图关闭时缩放百分比只在绘制地图时更新，实际 46% 仍显示 100%；改为镜头更新时同步文字，已在真实浏览器确认。
- `PARLOR_HTTP_TESTS=1 npm run check` 201 项全部通过，`npm run build` 通过；真实 HTTP/SSE、磁盘重启与回执去重均已执行。
- 新发现：只关闭独立 QA 服务后，已打开页面仍保留桌面，但刷新无法加载 HTML 入口。下一轮补齐离线入口缓存，并继续真实响应式与完整德州验收。没有操作既有 4175 服务或系统设置。

## 第十七轮：离线入口与完整版本缓存（0.12.0）

- 从真实失败入手：Node / Quick Tunnel 服务关闭时，浏览器刷新拿不到入口 HTML，原来的视图恢复代码无法运行。新增公开客户端的离线入口缓存，正常导航优先联网，失联或服务端故障时回到已校验的入口。
- Node 和静态构建生成同一份资源清单，HTML、13 个资源与 Worker 行为共同决定版本。安装先校验完整 SHA-256，再写入全部文件；失败清理本次缓存并保留旧版，激活后保留上一套供旧标签加载。
- 缓存只含公开入口、脚本和样式，安装不携带凭据，缓存键没有房主/个人口令或房间参数。API、SSE、私牌图片和跨来源请求保持原流程。HTTPS 与本机安全上下文注册，file 试玩跳过。
- 真实主客标签在服务停止后刷新，私牌投影、原位置与镜头保留；服务从临时检查点重启后自动恢复，客人成功继续抽牌。此前失败的浏览器路径现已通过。
- 208 项全量检查全部通过，含真实 HTTP/SSE；11 项定向缓存/静态检查、构建与差异检查通过。没有更改系统设置或既有服务，没有推送或部署。

## 第十八轮：响应式镜头与续接取景（0.12.1）

- 真实 1440→320 窗口切换发现：用户调整过镜头后只重绘小地图，旧平移坐标把牌桌留在屏幕外。现在保留取景中心与缩放，尺寸变化时平移镜头，并取消未完成拖动，避免松手误操作。
- 联机与试玩存档同时记录视口尺寸，在线、离线恢复都适配当前窗口；正常在线刷新也恢复原镜头。兼容旧记录，宽世界的 1.5%–10% 全景缩放不再被试玩恢复逻辑丢弃。
- 真实 320 宽度下牌堆留在视野中，刷新后坐标与缩放保持；历史和手牌面板关闭正常。实际洗牌中捕获三张牌的动画变换，另一个标签收到一次顶部通知。
- 新增三项取景/恢复回归，18 项定向检查、211 项全量检查全部通过，构建通过。用户随后要求将德州改为纯资源并补齐叠牌物理交互，下一轮按这个新方向修改。

## 第十九轮：自由实体资源与牌堆（0.13.0）

- 移除德州专用面板、快捷键、下注和结算程序；资源库提供 52 张扑克、桌垫、1/5/25/100/500 筹码、庄家钮和可编辑规则指引。筹码由玩家自行取用与移动。
- 单牌、散牌组和整叠可拖到公共牌或牌堆上，显示「叠到顶部」后原子合并。保留每张牌的正反面、角度、原牌背及来源图片；混合两种牌盒后仍能正确显示。合并后的主牌堆不再留下重复可操作实例。
- 洗牌只打乱顺序。牌堆投影和图片接口只公开最上方应当可见的一张；正面朝上的牌被覆盖后隐藏，洗到顶端时显示。取到桌上、横向/纵向/网格展开以及收回散牌均保留朝向，拖动预览也保持顶牌角度。
- 锁定的牌、牌堆、筹码和桌垫在悬停或键盘聚焦时显示淡灰锁头；提示不截获指针事件。牌堆的位置锁仍允许抽牌、洗牌和接收卡牌。
- 旧德州完整存档与房间检查点转换为自由资源：原牌、私人区和牌序保留，专用锁解除；余额与已下注额转成普通筹码标记并记录原数额。新文件不再保存规则程序状态。
- 双引擎与生产指针事件检查覆盖插入顺序、混合来源、正反面、展开、锁定、回退、存档与迁移。三客户端检查丢失入堆回执后离线刷新、磁盘重建和重试，确认没有重复放牌或暴露私有牌面。
- 真实 4187 主客标签通过正面牌叠回、背面遮挡、洗牌后展开仍朝上、54 + 52 张合成 106 张、散牌成叠、纵向展开与收回，以及客人自行移动筹码与庄家钮。规则指引正文完整显示；真实 hover 锁头透明度为 0.72，锁定拖动不改变位置。
- 390×844 与 320×740 实测发现「更多」换行后菜单越出屏幕左边；改为相对操作条定位。修复后菜单完整留在屏幕内，打开手牌抽屉时限制菜单高度，保留滚动空间。
- 现有 4186 / QAB-926 备份后升级，核对 274 张牌、16 张私牌、3 个席位、原牌序、位置、身份及回执保留；3000 旧德州筹码转为普通标记。实际房主页面在线且无浏览器错误。收尾审查修复「取到桌上」按钮归零角度，生产点击回归覆盖取牌后的原角度。
- `PARLOR_HTTP_TESTS=1 npm run check` 212 项全部通过，含真实 HTTP/SSE；构建通过，静态契约为 171 个 DOM ID / 12 个客户端资源。原线上地址、跨设备公网与原生触摸尚未实测，没有推送或部署。

## 第二十轮：公网空桌与 HTTP 同步兜底（0.13.1）

- 用户实际开房后界面出现但牌桌和资源库为空。对比确认普通状态接口正常返回，公网 SSE 没有首包；原客户端仅在事件流报错后检查状态，无法处理一直挂起的流。
- 入座响应直接附带当前玩家有权查看的桌面。独立 HTTP 同步通道在 SSE 挂起或断线时接续牌桌、指针、拖动、标记、选牌请求与聊天；健康的 SSE 恢复后停止轮询。失败请求退避重试，定期重试事件流；普通请求有超时与并发保护。
- 无变化的桌面不重复发送，指针合并成各玩家最新位置，临时信号有数量和时效上限。旧服务仍可用状态接口恢复桌面，迟到的初始响应不会覆盖更新的操作回执。
- 新增 8 项生产客户端与协议回归，覆盖永不返回的流、仅 OPEN 的流、首次资源库、连续同步、私牌与拖动保密、聊天及请求、离线恢复、旧服务兼容和迟到响应。替换只匹配旧提示表达式的静态断言，由实际行为检查承担回归。
- `PARLOR_HTTP_TESTS=1 npm run check` 220 项全部通过，构建通过。独立真实公网主客标签看到资源库，筹码与抽牌结果同步；客人抽到的红桃 J 对房主保持牌背，聊天传递成功。
- 用户原 4173 / 4AT-3SK 房间先备份再正常关闭重启，保留 54 张牌、原牌序、朝向、席位和凭据。新公网入口的资源库为 22 项，全部可取用，无浏览器错误。既有 4175 与 4186 服务保持运行，未修改系统网络设置。不同设备和不同网络仍未实测。

## 第二十一轮：无衬线牌面（0.13.2，独立预览）

- 牌面、牌背、资源缩略图与界面字标统一使用本地无衬线字体。扑克使用大号点数与花色，10 单独调整字距与字号；大小王采用竖排角标，保留空心与实心星形。牌背改为细格纹与正体字母。
- 保留 94 × 138 的牌尺寸、原资源颜色、自定义图片与全部玩法交互。UNO 保留彩色牌面和原符号。
- 在独立本地预览检查红黑花色、A/2/7/9/10/J/Q/K、大小王、UNO 功能牌、牌背、手牌抽屉、资源库与放大查看。390/320 宽度的独立 iframe 发现工具栏遮住手牌角标，调整窄屏抽屉层级后牌角与标题完整显示；未调整用户浏览器窗口。
- `PARLOR_HTTP_TESTS=1 npm run check` 220 项全部通过，`npm run build` 通过。视觉样张由生产牌面渲染函数生成；原生触摸与其他设备尚未验收。
- 改动仅在 `codex/sans-card-faces` 的独立工作目录。运行中的 4173 公网房间及原工作目录未修改、未重启，本轮未合入或部署。

## 第二十二轮：自由回合、亮手牌与顺滑拖放（0.14.0，独立预览）

- 房主与当前回合者都能转交行动标记；当前玩家可在顶部直接「结束回合」，工具中也可指定接手玩家。其他客人不能代替当前玩家转交，随机指定仍由房主操作。
- 手牌可在私人区或手牌抽屉原地展示、收回展示，保留位置；只有持牌人可以翻面。投影与图片接口同步控制可见性，收回后其他玩家再次只能看到牌背。移动、整理与完整对局存档保留展示状态，转交他人时自动收回；旧存档默认不展示。
- 主桌与资源桌垫面积扩大到此前约 2.25 倍，新私人区围绕更大的主桌布置，镜头与资源缩略图适配实际边界；既有明确摆放的私人区坐标保留。
- 筹码可叠到其他筹码顶部，整叠移动、展开、装袋或取出最上面一枚。每枚保留身份、颜色与面额，混合面额也可叠放；不增加发筹码、计价或结算规则。锁定检查、撤销与检查点恢复覆盖整组操作。
- 本人松手后预览停留在落点，收到确认再接上正式状态，不再先回原处。不同资源可以连续拖放；待确认的来源与目标暂不重复操作。其他玩家收到独立的落点信号，指针离开不会提前清除它；完成信号与桌面版本对齐，迟到消息不重建旧预览。丢失回执重试原操作，明确失败才回到已确认位置。
- 聊天入口与 `C` 打开轻量浮动输入框，拖动框头或方向键移动，窄屏限制在视口内；时钟按钮打开全部记录。两个输入框共享草稿，关闭、发送失败和等待期间继续输入均保留内容；资源延迟加载不会抢走聊天时的选择。
- 位置标记仅保留一个涟漪，发送者与接收者显示一致；行动标记下方的动态提示不遮挡「结束回合」。
- `PARLOR_HTTP_TESTS=1 npm run check` 238 项全部通过，无跳过，包含真实 HTTP/SSE、服务端与生成浏览器引擎一致性、生产指针事件和聊天交互回归；`npm run build` 与 `git diff --check` 通过。延迟、断线重试、回执乱序和失败回退使用受控 DOM 与协议环境验证。
- 独立 4188 房间的真实房主、客人标签验证亮牌与收回的双向可见性、客人结束回合、聊天同步及单涟漪；浏览器验证筹码叠放、整叠移动与取单枚。390/320 宽度独立 iframe 检查聊天框与工具栏，未调整用户浏览器窗口。不同设备、原生触摸与本轮公网体验尚未实测。
- 改动仅在 `/Users/awu/Documents/ChatGPT/table-game-canvas-card-preview` 的 `codex/sans-card-faces` 分支。用户正在玩的 4173 / 4AT-3SK 服务及原 `main` 工作目录保持原样，本轮未合入、未部署、未重启当前房间。

## 第二十三轮：桌面多选与整组整理（0.15.0，独立预览）

- 桌面端按住 `Shift` 点击物件加入或移出多选，也可按住 `Shift` 从空白处拖框，将完整框住的公共资源加入选择。松开 Shift 后拖动任一已选物件整体移动；点空白处或 `Esc` 取消。已选轮廓与底部数量提示保持同步，普通空白拖动仍用于平移镜头。
- 公共牌、牌盒、筹码、骰子、便签、袋子与桌垫可以混合选择；点选重叠牌或筹码时包含原有整叠。框选略过手牌、锁定物件和待确认物件，手牌原有的 Shift 背面盖放手势保留。多选期间隐藏容易误触的翻面、牌盒菜单与计数按钮。
- 一组移动只提交一次操作、保存一个撤销点，保留每件物品的位置关系、朝向、正反面、层级和容器内容。整组平移不触发自动叠牌、装袋或交付手牌；边界限制使用共同位移，旋转的牌和物件按实际外接矩形计算。锁定、消失、隐藏和已被他人移动的成员会让整组操作失败，不会只移动一部分。
- 本人及其他玩家沿用落点预览与确认衔接。多人信号只携带经过服务端校验的公共资源引用与偏移，牌面仍来自各自有权查看的投影。批量操作等待回执时保护所有成员，刷新后也恢复这些等待状态；重试复用原操作编号，避免再次移动。
- 新增服务端与生成浏览器引擎回归、生产 DOM 指针与协议回归，覆盖混合资源、堆叠成员、加选减选、框选、缩放与平移后的坐标、取消与捕获丢失、原地盖牌手势、锁定与隐私、整组边界、撤销、存档、并发变化、丢失回执和刷新重试。`PARLOR_HTTP_TESTS=1 npm run check` 254 项全部通过，无跳过；`npm run build` 与 `git diff --check` 通过。
- 真实独立浏览器验证 Shift 加选、减选、三种资源整体拖动与 Esc 取消；4188 测试房间的房主和客人页面显示相同落点，刷新保留位置，一次撤销恢复全部三个物件，双方无浏览器错误。原生组合键框选和手机触摸本轮未实测；框选由生产指针事件回归覆盖。
- 改动仍仅位于 `/Users/awu/Documents/ChatGPT/table-game-canvas-card-preview` 的 `codex/sans-card-faces` 分支。仅更新独立 4188 测试服务，用户的 4173 / 4AT-3SK 公网房间、隧道及原 `main` 工作目录未修改、未重启；未合入或部署。

## 第二十四轮：减少同步重绘与分组扫描（0.16.0，独立预览）

- 玩家列表、头像栏和私人区仅在实际内容改变时重建。筹码、便签、桌垫等只移动位置时复用原节点，保留焦点和图像；锁定、点数、文字与权限变化仍刷新对应外观。
- 牌堆数量标记不再随每次同步删除重建；散开后及时恢复单牌悬停说明。多选使用当前投影的资源索引，筹码通过相邻空间分组，避免为每枚筹码反复扫描全桌。服务端与浏览器共用分组实现。
- 可复现的生产客户端 DOM 替身场景（181 枚标记）：无变化重绘创建元素从 31 个降为 0 个，单枚移动从 42 个降为 7 个。240 枚分散标记的完整分组少于 3000 次坐标读取；这些指标是计算/DOM 工作量，不代表真实浏览器帧率。
- 新增四项行为与工作量回归；`PARLOR_HTTP_TESTS=1 npm run check` 258 项全部通过，无跳过，构建与差异检查通过。真实独立预览验证连续移动筹码、便签后位置和操作正常，浏览器无错误。
- 原 4173 / 4188 服务本轮开始时均未监听；仅在独立工作目录启动新的 4188 / NW7-3K8 测试房间，未操作用户房间、隧道或原 `main` 目录。计划见 [NEXT_ITERATIONS](./NEXT_ITERATIONS.md)，后续继续四套实体棋类资源与资源库体验。

## 第二十五轮：四套自由棋类资源（0.17.0，独立预览）

- 资源库新增套装入口：国际象棋与象棋各 32 子，斗兽棋 16 枚动物，飞行棋四色各 4 架飞机。每套包含实际棋盘和可编辑的规则指引；飞行棋另带六面骰。棋盘、棋子和指引也能单独拿取。
- 国际象棋提供 8 × 8 坐标棋盘与六种无字体依赖的棋子轮廓；象棋按交点摆放，保留九宫、河界和炮兵标线；斗兽棋标明河流、陷阱与双方兽穴，动物含等级和名称；飞行棋有 52 格环路、四个机场和起飞点、各色六格终点航道及 12 格飞跃虚线。所有文字沿用无衬线字体，图形本地生成，随公开离线入口缓存。
- 新增的 `spawn-set` 仅一次摆好声明式资源。任何在座玩家都可取用、任意移动、复制和收纳棋子，不自动执行走法、吃子、升变、计时或胜负。整套先校验容量与边界，一次创建、一次撤销，重复取用产生独立实例。
- 棋子外观通过已知资源定义还原，保存、恢复、装袋和重新取用不丢失样式或身份。新增服务端/浏览器一致性、初始摆位、容量原子性、边界、自由走子、复制、容器与存档检查，生产客户端覆盖全部套装和丢失创建回执后的双端去重。
- 真实独立预览逐套查看棋盘与棋子；国际象棋走马、象棋走兵、斗兽棋鼠入河、飞机移到起飞点与掷骰均可操作，页面无错误。发现并修复规则便签仅显示头部的问题；飞行棋指引实际 `clientHeight` 与 `scrollHeight` 同为 370，正文无裁切。
- `PARLOR_HTTP_TESTS=1 npm run check` 272 项全部通过、无跳过；构建与差异检查通过。更新原先固定 22 件资源/12 个客户端文件的检查，现为 80 件资源/13 个文件。双端新套装去重使用生产协议替身；新版真实联机双端将在下一轮串联复核。
- 仍只更新独立分支和 4188 测试服务，未修改用户原房间或原 `main` 工作目录，未合入、推送或部署。下一轮处理大棋盘取景、避免覆盖已有桌面以及资源库查找。

## 第二十六轮：资源库按需绘制与棋盘导航（0.18.0，独立预览）

- 资源库保留全部可用条目，关闭时不生成缩略图。打开后使用以资源库为根的 IntersectionObserver 绘制可见区域及附近预览，键盘焦点也可触发绘制；不支持观察器时在打开资源库后生成。切换分类复用条目和已完成的图，过滤后迟到的回调不会绘制已移出的条目。
- 套装新增「查看单件」，包含棋盘、指引与各类补充棋子；飞行棋也包含通用六面骰。提供搜索数量、清空搜索、返回全部资源及空状态入口，分类支持方向键、Home 和 End。收藏和等待取用数量更新时保留按钮与焦点。
- 点击大棋盘或套装时按完整外框寻找空处，避开公共资源的旋转外框、私人区及排队/正在添加的资源。发送排队请求前再次检查位置，整套指引和骰子也纳入预留；搜索有次数上限。没有空间时提示手动拖出，显式拖出始终保留玩家指定落点，不移动已有物件。
- 新增棋盘、桌垫和多选的聚焦操作，`Shift + F` 聚焦所选，`F` 保持全桌视野；桌面导航也按棋盘完整边界取景。取景避开侧栏、操作条和手牌抽屉，支持旋转后的外框。棋盘边框和标题可拖动，内部空白处用于平移镜头或 Shift 框选，棋子仍可自由移动。
- 取用成功后仅在玩家意图与镜头均未改变时进入新棋盘视野。等待期间缩放再返回原视野、选择其他资源、框选、输入聊天或打开弹窗都会受到保护；资源库打开时的延迟焦点也不能抢回后续输入。成功取用沿用顶部同桌动态和等待数量，去掉重复的底部成功提示。
- 与 `d78b361`（0.17.0）对比，同样保留 80 条资源，关闭资源库时其 DOM 后代从 2336 降至 824，约减少 65%；初始缩略图从 80 张降至 0 张。按测试指定的六个可见预览绘制后为 1376 个后代。这是生产脚本在 DOM 替身中的确定性节点计数，不是浏览器帧率或延迟；记录在 `.parlor/card-review/library-0.18.0-benchmark.json`。
- 新增客户端交互检查覆盖按需绘制/缓存、套装单件、焦点与键盘、空位与边界、连续取用及失败、显式重叠摆放、满桌拒绝、边框移动、盘内框选、旋转取景和迟到回执。真实 HTTP/SSE 双席位检查一次取用四套棋、创建回执去重、批量移动、聊天、撤销与私牌隔离。`PARLOR_HTTP_TESTS=1 npm run check` 282 项全部通过、无跳过，日志在 `.parlor/card-review/check-0.18.0.log`；构建和差异检查通过。
- 本轮提交时真实浏览器复核受 Mac 锁屏影响而延后；解锁后完成资源库、实际边框命中与棋盘取景检查，并发现原生聚焦会额外滚动画布。该问题和双端复核已在后续 0.18.1 完成，见下文。

## 第二十七轮：键盘定位与画布坐标修复（0.18.1，独立预览）

- 原生 Tab 切到屏幕外手牌时，浏览器曾把画布 `scrollTop` 改成 197，而应用镜头未变，导致画面与拖放坐标脱节。世界画布改为 `overflow: clip`，资源库和手牌抽屉继续使用各自的滚动区域。
- 键盘聚焦屏幕外物件时通过现有镜头定位，不改变选择与公共状态；保留当前缩放，大棋盘必要时缩小，并避开侧栏、操作条与手牌抽屉。Tab 意图只消费一次，在指针输入、窗口失焦或本次事件结束后清除；普通点击、聊天焦点、同步重绘和正在进行的拖动/平移不移动镜头。
- 原生浏览器连续 38 次 Tab，包括私人牌及其翻面按钮，画布与父容器滚动位置始终为 0。Shift 选中两架飞机后拖动，两枚棋子的位移一致，未选棋子与镜头不变，无残留拖影。第二套棋盘自动避开第一套，原物件位置与角度保留；打开手牌后聚焦棋盘，棋盘底边在 330px，操作条与手牌分别从 365px、435px 开始，无遮挡。
- 独立 4188 / NW7-3K8 房主与新客人标签完成真实联机验证：客人取用完整飞行棋、双端显示相同整组落点、房主一次撤销恢复全部成员、聊天送达；远端取用和移动期间房主的输入焦点、草稿与镜头保留。双方各抽一张牌，各自只看到自己的牌面，对方只见牌背。三个测试页面控制台无错误或警告。
- 新增三项生产客户端事件回归，DOM 替身增加捕获阶段以覆盖指针事件停止冒泡时的焦点意图清理。`PARLOR_HTTP_TESTS=1 npm run check` 285 项全部通过、无跳过；构建与差异检查通过。日志与原生验收摘要位于 `.parlor/card-review/check-0.18.1.log`、`.parlor/card-review/native-0.18.1.json`。
- 完成 `NEXT_ITERATIONS.md` 的本轮计划，每版独立提交。仅更新独立分支及 4188 测试服务，未合入或部署到用户原房间；本版跨设备公网和原生触摸体验尚未实测。

## 第二十八轮：按资源类型批量整理与飞行棋盘面修正（0.19.0，独立预览）

- 多选操作条按内容显示动作。混合资源可收回资源库、统一锁定/解锁与聚焦；卡牌和牌盒可合叠、洗牌、横向/纵向/网格展开、统一翻到正面或背面；棋子和筹码可合叠及展开。洗牌仅处理选中成员，保留每张牌的朝向、角度、来源牌背与图片，牌盒外未选中的手牌保持私有。
- 「收回资源库」先准备完整保存方案，再一次移除所选桌面实例。修改过的便签等物件保留为共享模板，卡牌与牌盒内容按来源保存为可取用牌组；与已有模板或内置同款比较，避免重复收藏。锁定成员、非空袋子或收藏容量不足会使整批失败，桌面保持原状。外部手牌及袋内牌继续引用有效来源牌盒；一次撤销同时恢复桌面和资源库。
- 批量命令携带成员位置与牌盒数量/顶牌校验，旧选择遇到并发变化时整批拒绝。所有成员在等待确认与刷新重试期间受保护，避免继续抽取或修改同一批资源。洗牌重新编号、展开牌盒后通过持久回执恢复准确选择；玩家已换选其他物件时不抢回选择。
- 根据用户提供的盘面参考重画飞行棋：采用 17 单位方盘中的 52 格连续矩形/三角色块环路，校正四色停机坪与六格终点通道，中央为四色三角终点。左上黄、右上绿、左下红、右下蓝；红向右、黄向下、绿向左、蓝向上的四条飞行虚线各跨越 12 格，穿过异色终点通道的第三格。移除旧圆点十字路线和编号，以白色落点、方向箭头与飞机图形标示；所有文字使用无衬线字体。新套装飞机摆在对应停机位，不重排已经在桌上的棋子。
- `PARLOR_HTTP_TESTS=1 npm run check` 306 项全部通过、无跳过，包含双引擎批量原子性、牌面与牌背保留、库容量与去重、回执丢失、刷新恢复、手牌隔离，以及飞行棋连续相邻格共边、三角转弯和四条参考航线的校验；构建与差异检查通过。
- 真实 1280 × 720 独立试玩验证棋子合叠/展开、混合收回、改写便签重新取用和整批撤销；16 架飞机各自对齐不同的同色停机位，实际中心误差小于 0.003 CSS 像素。批量菜单完整显示。4188 / NW7-3K8 房主与客人双端验证两张一正一反公共牌洗牌后朝向保留、展开及统一翻面、收回成两张牌资源并重新取用、牌盒加散牌合并洗牌与房主一次撤销；双方原有私牌保持隔离，未选牌盒数量不变。三个页面均无控制台错误或警告。
- 检查日志与原生验收摘要位于 `.parlor/card-review/check-0.19.0.log`、`.parlor/card-review/native-0.19.0.json`。仅更新独立工作目录和测试服务，未合入、推送或部署到用户原房间；跨设备公网与原生触摸本轮未实测。

## 第二十九轮：指定玩家发牌面板（0.20.0，独立预览）

- 牌盒、公共牌堆和纯卡牌多选共用发牌面板；普通玩家也可操作，工具面板提供「指定发牌」。按席位显示收牌人、在线状态、已有手牌和预计收牌量，默认只选在线玩家，支持全选、仅在线与清空。
- 支持每人定量、平均分配、随机抽牌及从牌顶依次发。平均分配默认保留余牌，也可随机分完；提交前同步展示每人张数、共发数量与余牌，张数无效或牌不够时直接在面板提示。
- 浏览器与服务端共用分配计算和发牌命令。只处理所选牌源，未发出的牌保留原身份、牌序、位置与正反面；进入手牌的卡牌保留图案与旋转角度，更新身份并恢复私有可见性。锁定的公共牌需先解锁，固定位置的牌盒仍可发牌。
- 一次发牌只创建一项操作记录，明确写入操作者、牌源、每位收牌人实际张数和余牌；整次发牌可由房主一次撤销。校验离席玩家、重复收牌人、牌数及来源变化，失败不产生部分发牌。
- 在同步更新时保留已输入张数和复选框焦点，新入席者不被自动加入，离席与牌数变化会提示核对。待确认命令保护所有来源；丢失回执、刷新重试和服务端重启不会重复发牌。收起面板后迟到的回执不抢走聊天焦点。
- 新增 21 项核心、协议及生产客户端事件回归；`PARLOR_HTTP_TESTS=1 npm run check` 共 327 项通过、无跳过。最终静态契约、构建和差异检查通过；原生浏览器验证两人和四人面板、平均余牌预览、指定收牌、普通玩家操作、双方私牌隔离、实时数量与焦点保留、一次撤销及刷新恢复。
- 1280 × 720 下四人定量面板完整显示，按钮与统计固定可见；三个验收页面无控制台错误或警告。日志和原生验收摘要位于 `.parlor/card-review/check-0.20.0.log`、`.parlor/card-review/native-0.20.0.json`。继续仅更新独立目录与 4188 测试服务；用户原房间未切换，公网跨设备和原生触摸本轮未实测。

## 第三十轮：鼠标旁的快捷短语（0.21.0，独立预览）

- 左侧「短语」或 `T` 打开紧凑面板，点选或按 `1–9` 发送。保留用户指定的七条原文，补充「稍等一下」「轮到你了」，支持方向键、Home/End、Esc 和焦点归还。关闭面板保留当前资源选择，聊天输入、弹窗与正在进行的桌面手势不触发发送。
- 每位玩家只有一个短语气泡，跟随现有光标同步，约 5 秒后淡出；新短语替换上一条。气泡绘制在缩放世界之外，保持无衬线字号，靠近边缘时换侧，并且不拦截鼠标。测量只发生在创建和尺寸变化时，指针移动复用节点及尺寸；提供屏幕阅读器播报与减少动态效果支持。
- 联机使用独立临时信号，服务端从已验证席位取得发送者姓名和颜色，只接受已知短语并限制位置与频率。发送不改变牌面、回合、历史、撤销或存档，也不广播完整桌面；普通玩家和房主都可使用。离线试玩共用短语与验证逻辑。
- SSE、HTTP 回执和备用轮询共用事件去重；轮询仅送达每位玩家最新的未过期短语，重连不重播。按房间时钟处理剩余显示时间，旧对局与乱序消息不会盖掉新气泡。广播确认可释放发送状态，即使 HTTP 回执丢失也不误报失败；发送确实失败时提示错误，迟到结果不抢聊天焦点。
- 新增 13 项核心、传输及生产客户端事件回归；`PARLOR_HTTP_TESTS=1 npm run check` 共 340 项通过、无跳过。覆盖准确文本、身份验证、频率、位置、无游戏副作用、两个同步通道、去重与过期、键盘和焦点、边缘避让、无额外布局读取、席位切换、时钟偏差及丢失回执。
- 原生浏览器在 464 × 837 预览与 1280 × 720 双席位验证：长短语完整显示，`T + 5` 发送，鼠标移动 60 × 55 px 时本地气泡同幅移动；客人的「跟注」「ALL IN！」「开牌！」传到房主，远端气泡随光标更新，两人可同时显示，随后清除。Esc 保留已选牌盒，聊天中键入 `t5` 只进入草稿；三个验收页面无控制台错误或警告。
- 完整日志与原生摘要保存在 `.parlor/card-review/check-0.21.0.log` 和 `.parlor/card-review/native-0.21.0.json`。仅更新独立分支和 4188 测试服务，原线上房间及其隧道未切换；本轮未做跨设备公网及原生触摸验证。

## 0.6.0 与更早的验证记录

2026-09-06：`npm run check`、`npm run build`、`git diff --check` 通过；121 项自动化测试全部通过。

- 核心、规则包与资产校验；服务端和生成的浏览器核心执行相同资源场景。
- 协议层双客户端投影、独立牌盒、袋内资源不可见、拖动消息过滤、存档权限与会话保留。
- 24 项客户端事件测试加载真实 HTML 和全部生产脚本，覆盖牌盒添加与拖动、卡组装袋、手牌抽屉、取消、编辑、骰子、收藏、聊天、同 ID 导入与移除、旧记录兼容和存储失败，以及菜单与转交状态保留、小地图键盘导航、历史面板焦点恢复、双指镜头计算、换指、手牌方向区分和收到新牌时的交互连续性。
- 静态契约校验 143 个 DOM ID、脚本依赖顺序、ARIA 引用、图标与资源文件。

客户端事件测试使用没有布局能力的内存 DOM 和存储替身；协议测试使用请求与响应替身。它们不能证明真实浏览器视觉效果、触摸操作、真实 IndexedDB、本地 HTTP 或公网双人联机已经通过。

## Git 提交与恢复记录

用户已授权每轮验证后自动 commit。前六轮完整的 0.4.0 状态已包含在提交 `0c2bd633e64d8c8d2f51894c925f37a69c38886e`（`feat.update`）中，47 个文件与当时已验证的检查点逐一一致。

工具此前的 Git 提交请求被阻断：沙箱无法创建 `.git/index.lock`，提升权限的请求又因审批接口不支持 `codex-auto-review` 而返回 404。上述实际提交是在读取仓库时发现并核验的，不能归因于被拒绝的工具执行。第七轮随后已出现在提交 `7924342`（`feat.ver1`）中，该提交也包含第八轮的核心回执契约；第八轮完成对应客户端队列与回归测试。0.6.0 的基线为 `e1d7b45`（`feat.ver2`）。2026-09-07 再次读取发现 `a35a28a`（`ver3`），已核验其 45 个文件的内容及模式与 0.11.1 检查点完全一致；0.7–0.11.1 已包含在该提交中，该提交同样不能归因于此前被拒绝的工具操作。审批通道未恢复时保留补丁与日志，不绕开拒绝写入 Git。

本地 `.iterations/2026-09-06/` 保存分轮补丁、校验值与验证日志。第五、六轮补丁均相对旧基线累计，不能依次叠加；第七轮以 `0c2bd63` 为基线单独保存。

## 真实浏览器验收进度

当前权限已恢复，真实主客标签、IndexedDB 和本地 HTTP/SSE 已部分通过，见第十六轮记录。历史审批失败不再是当前阻断。

桌面、390/320 宽度菜单、服务关闭后的离线刷新、自由叠牌与资源取用已有实际证据，见第十六至十九轮及 [ACCEPTANCE](./ACCEPTANCE.md)。原线上地址、跨设备公网与原生触摸尚未实测。
