# 连续体验迭代 · 2026-09-07

继续既有 0.6.0，自由桌游空间为主，保留绿色桌布、随选择出现的操作与私有投影。

## 完整范围与证据

| 目标 | 实现方向 | 当前证据 |
| --- | --- | --- |
| 简单洗牌动画 | 一次操作事件触发本地叠牌动画，减少动态效果时立即结束 | 0.7 已接入；事件去重和自动清理测试通过，视觉待验 |
| 同桌操作、聊天短消息 | 顶部显示操作者颜色，点击进入历史或聊天，超时清理 | 0.7 生产脚本事件测试通过 |
| 历史方便开关、按玩家审计 | 侧栏 + L；玩家、类别、关键词过滤；导出筛选记录 | 0.7 已实现，保留最近 500 条；改名前后按同一玩家 ID 筛选 |
| 修改昵称、移除桌边状态 | 点自己的昵称编辑，重名自动区分 | 0.7 已实现，身份与手牌归属不变 |
| 简化 UNO | 数字、禁止、反转、万能符号；中文无障碍名称 | 0.7 已实现，牌面无英文动作缩写；视觉待验 |
| 私人区自由摆放 | 私有牌保留世界坐标；区域内自由移动、旋转，抽屉作为快捷操作 | 0.8 双引擎与生产拖动测试通过，真实触摸待验 |
| 请求选择对方私牌 | 同色、8 秒、多张标记，不暴露牌面；由持有者交付 | 0.8 已接入；过期、无权操作、混合归属、锁定的整体拒绝已测试 |
| 远端拖牌可见及开销 | 10 Hz 合并坐标，位置过渡，持有中保活，防止结束后残留 | 0.8 协议与 DOM 复用测试通过；108 张牌的消息小于 500 字节；网络实测待验 |
| 断线恢复原桌面 | 原席位恢复、命令确认与去重、延迟和重试状态 | 0.12 补齐离线入口，真实主客标签在服务停止后刷新保留桌面，重启后自动续接并继续抽牌；回执和多身份回归通过 |
| 完整对局存档、隔天继续 | 保留座位、手牌、牌堆顺序和桌面；与布置模板分开 | 双端往返、私有投影、磁盘故障、原子备份与检查点恢复通过；真实浏览器保存、刷新、带备份恢复与重开席位通过 |
| 德州扑克 | 52 张牌、筹码、盲注与下注、边池、公共牌、摊牌和续局 | 0.10 已实现；0.11.1 三客户端完整手、丢失下注回执及磁盘重建后续局串联检查通过；真实联机待验 |
| 试玩刷新恢复 | 自动保存完整桌面、镜头、查看身份；多页冲突另存副本 | 0.11 生产脚本与存储替身通过，真实浏览器耐久性待验 |
| 光标故障复查与启动链接 | 检查导航、窗口切换及断线后的操作，启动时保留主客链接 | 0.11.2 生产事件及协议回归通过；补上资源缓存更新与两条启动链接，真实线上复现待验 |
| 连续多轮优化 | 每轮测试、构建、迭代记录和检查点 | 0.7–0.11.1 已核验包含在 `a35a28a`（`ver3`）；0.11.2 已提交 `d7a8c0e`；0.11.3 修复真实浏览器发现的缩放标记 |

## 验证边界

0.12.0：`PARLOR_HTTP_TESTS=1 npm run check` 208 项全部通过，构建通过。独立主客浏览器已验证私牌自由拖动、请求转交、通知、改名、历史筛选、完整存档与重开席位；小地图关闭时缩放标记不更新的问题已修复。

此前审批服务 404 的阻断已随当前权限恢复解除；真实 HTTP/SSE 两项检查通过。离线入口缓存已补齐，服务停止时两端刷新和重启自动恢复通过。响应式、原生触摸、德州真实浏览器完整一手和公网联机仍按 [ACCEPTANCE](./ACCEPTANCE.md) 继续。

`.iterations/2026-09-07/` 保存 0.7、0.8、0.9、0.10、0.11 和 0.11.1 的检查点，都是相对 `e1d7b45` 的累计补丁。0.11.2 单独保存相对 `a35a28a` 的增量；各份累计补丁不能顺序叠加。
