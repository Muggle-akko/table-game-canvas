# 连续体验迭代 · 2026-09-07

继续既有 0.6.0，自由桌游空间为主，保留绿色桌布、随选择出现的操作与私有投影。0.13.0 按用户后续要求统一为实体资源操作：程序负责同步、容器和可见性，玩法与筹码分配由玩家决定。

## 完整范围与证据

| 目标 | 实现方向 | 当前证据 |
| --- | --- | --- |
| 简单洗牌动画 | 一次操作事件触发本地叠牌动画，减少动态效果时立即结束 | 事件去重和自动清理测试通过；0.12.1 真实浏览器已捕获洗牌动画和客人通知 |
| 同桌操作、聊天短消息 | 顶部显示操作者颜色，点击进入历史或聊天，超时清理 | 生产脚本事件及真实双标签通过 |
| 历史方便开关、按玩家审计 | 侧栏 + L；玩家、类别、关键词过滤；导出筛选记录 | 0.7 已实现，保留最近 500 条；改名前后按同一玩家 ID 筛选 |
| 修改昵称、移除桌边状态 | 点自己的昵称编辑，重名自动区分 | 0.7 已实现，身份与手牌归属不变 |
| 简化 UNO | 数字、禁止、反转、万能符号；中文无障碍名称 | 0.7 已实现，牌面无英文动作缩写；视觉待验 |
| 私人区自由摆放 | 私有牌保留世界坐标；区域内自由移动、旋转，抽屉作为快捷操作 | 0.8 双引擎与生产拖动测试通过，真实触摸待验 |
| 请求选择对方私牌 | 同色、8 秒、多张标记，不暴露牌面；由持有者交付 | 0.8 已接入；过期、无权操作、混合归属、锁定的整体拒绝已测试 |
| 远端拖牌可见及开销 | 10 Hz 合并坐标，位置过渡，持有中保活，防止结束后残留 | 0.8 协议与 DOM 复用测试通过；108 张牌的消息小于 500 字节；网络实测待验 |
| 断线恢复原桌面 | 原席位恢复、命令确认与去重、延迟和重试状态 | 0.12 补齐离线入口，真实主客标签在服务停止后刷新保留桌面，重启后自动续接并继续抽牌；回执和多身份回归通过 |
| 完整对局存档、隔天继续 | 保留座位、手牌、牌堆顺序和桌面；与布置模板分开 | 双端往返、私有投影、磁盘故障、原子备份与检查点恢复通过；真实浏览器保存、刷新、带备份恢复与重开席位通过 |
| 德州扑克资源 | 52 张扑克、桌垫、五种筹码、庄家钮、可编辑规则指引，玩家自由取用 | 0.13 移除旧程序规则；真实主客页面取用和客人自由移动通过，旧存档迁移检查通过 |
| 放回顶部、合堆与展开 | 单牌/散牌组/整叠自由叠放，保留正反面、角度、顺序和来源；横向/纵向/网格展开 | 双引擎、三客户端重试与磁盘续接通过；真实浏览器合堆、洗牌朝向、展开和收回通过 |
| 锁定状态更明确 | hover/键盘聚焦显示淡灰锁头，不截获指针事件 | 牌、牌堆、小物、桌垫事件检查通过；真实悬停与锁定后拖动通过 |
| 试玩刷新恢复 | 自动保存完整桌面、镜头、查看身份；多页冲突另存副本 | 0.11 生产脚本与存储替身通过，真实浏览器耐久性待验 |
| 光标故障复查与启动链接 | 检查导航、窗口切换及断线后的操作，启动时保留主客链接 | 0.11.2 生产事件及协议回归通过；补上资源缓存更新与两条启动链接，真实线上复现待验 |
| 连续多轮优化 | 每轮测试、构建、迭代记录和检查点 | 已推进至 0.13.0；0.12.1 修复真实响应式镜头，0.13 修复窄屏更多菜单定位 |

## 验证边界

0.13.0：`PARLOR_HTTP_TESTS=1 npm run check` 212 项全部通过，构建通过。独立主客浏览器已验证私牌自由拖动、请求转交、通知、改名、历史筛选、完整存档与重开席位。新增实际验收覆盖叠到顶部、54 + 52 张合堆、散牌成叠、保留正反面、自由筹码、指引排版和锁头。

真实 HTTP/SSE 两项检查通过。离线入口缓存已补齐，服务停止时两端刷新和重启自动恢复通过。390×844 与 320×740 的新版操作条和更多菜单已实测；原线上地址、跨设备公网和原生触摸没有计作通过，详见 [ACCEPTANCE](./ACCEPTANCE.md)。旧德州自动规则验收不再适用于当前产品。

`.iterations/2026-09-07/` 保存 0.7、0.8、0.9、0.10、0.11 和 0.11.1 的检查点，都是相对 `e1d7b45` 的累计补丁。0.11.2 单独保存相对 `a35a28a` 的增量；各份累计补丁不能顺序叠加。
